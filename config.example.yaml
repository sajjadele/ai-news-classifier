# AI News Classifier — Configuration
# Copy this file to config.yaml and fill in your credentials.

# API endpoint (OpenAI-compatible)
api_base: "https://your-openai-compatible-api/v1"

# Your API key (get from your provider)
api_key: "YOUR_API_KEY_HERE"

# Model to use for classification
model: "mistral-medium-3-5"

# HTTP proxy (optional)
# Leave empty if not needed
proxy: "http://YOUR_PROXY_HOST:PORT"

# Default RSS feeds to check
feeds:
  - name: "TechCrunch AI"
    url: "https://techcrunch.com/category/artificial-intelligence/feed/"
  - name: "The Verge AI"
    url: "https://www.theverge.com/rss/ai-artificial-intelligence/index.xml"

# Telegram Bot (Phase 4 — optional, for --publish)
# Get token from @BotFather, channel is your @channel or chat ID
telegram_token: "YOUR_BOT_TOKEN_HERE"
telegram_channel: "@your_channel"
